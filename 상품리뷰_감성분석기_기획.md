# 상품 리뷰 감성 분석기 — 에이전트 서버 프로젝트 기획서

> 수행평가 4 대체 과제: 「나를 위한 AI 직원 만들기」
> 선택 주제: ③ 상품 리뷰 감성 분석기 (난이도: 하)
> 대상: 코딩 초보자, 작업 기간 1~2일
> LLM: **Google Gemini API (무료 등급)**

---

## 1. 프로젝트 개요

쇼핑몰 리뷰 텍스트를 붙여넣으면 에이전트가 다음을 수행한다.

1. 리뷰를 장점/단점 문장으로 분류
2. 리뷰 내용만으로 감성 점수(1~5점) 산정
3. 사용자가 입력한 실제 별점과 감성 점수의 차이를 비교해 "과장 리뷰 여부" 리포트 생성

입력: 리뷰 텍스트 복사·붙여넣기 (+ 선택적으로 실제 별점 입력)
출력: 장단점 요약 + 감성 점수 + 과장 리뷰 리포트

---

## 2. 기술 스택 (Google 무료 등급 기준)

| 구성 요소 | 선택 | 비고 |
|---|---|---|
| LLM | **Gemini API** (`gemini-2.0-flash` 또는 `gemini-1.5-flash`) | Google AI Studio에서 무료 API 키 발급, 무료 등급 rate limit 내에서 사용 |
| SDK | `google-generativeai` (Python) | `pip install google-generativeai` |
| 도구 호출 방식 | Gemini **Function Calling** | Anthropic tool use와 동일한 개념, `tools` 파라미터로 함수 스키마 등록 |
| 서버/화면 | `streamlit` | 별도 프론트엔드 없이 서버+화면 겸용, 초보자에게 가장 쉬움 |
| 배포 | 로컬 실행 + 스크린샷 | 시간 부족 시 무료 호스팅(Streamlit Community Cloud)은 선택사항 |

### API 키 발급 방법
1. https://aistudio.google.com/ 접속 → 로그인
2. "Get API key" → "Create API key" 클릭
3. 발급된 키를 환경변수 `GOOGLE_API_KEY`로 저장 (코드에 직접 노출 금지, `.env` 사용 권장)

### 무료 등급 유의사항
- 분당/일일 요청 수 제한이 있으므로(모델별로 상이, 실행 시점에 Google AI Studio에서 최신 한도 확인) 테스트 시 과도한 반복 호출 피할 것
- 리뷰 텍스트가 너무 길면 토큰 제한에 걸릴 수 있으니 입력 길이 가이드(예: 2000자 이내) 안내

---

## 3. 에이전트에 등록할 도구(Tool) 3개

Gemini function calling 스키마로 아래 3개 함수를 등록한다.

### ① `classify_review_text` — 리뷰 텍스트 분류 도구
- 입력: 리뷰 원문(string)
- 출력: 장점 리스트(list[str]), 단점 리스트(list[str])
- 내부 로직: 프롬프트로 Gemini에게 "장점/단점을 분리해서 JSON으로 반환" 요청

### ② `score_sentiment` — 감성 점수화 도구
- 입력: 리뷰 원문(string)
- 출력: 감성 점수(1~5, float), 근거 요약(string)
- 내부 로직: 프롬프트로 "이 리뷰 내용만 보고 별점을 매긴다면 몇 점인지" 추정 요청

### ③ `detect_review_exaggeration` — 과장 리뷰(별점 괴리) 탐지 도구
- 입력: 실제 별점(int, 사용자 입력), 감성 점수(②의 결과)
- 출력: 괴리 정도(float, 실제별점 - 감성점수), 과장 여부(bool), 설명(string)
- 내부 로직: 두 점수 차이가 임계값(예: 1.5점) 이상이면 "과장 리뷰 가능성 높음"으로 판단하는 규칙 기반 로직 (LLM 호출 불필요, 순수 계산)

> 실행 흐름: 사용자가 리뷰 텍스트(+선택적으로 실제 별점) 입력 → Gemini 에이전트가 ①, ② 도구를 함수 호출로 실행 → 실제 별점이 있으면 ③ 도구까지 실행 → 결과 종합해서 화면에 출력

---

## 4. 단계별 실행 계획 (1~2일)

1. **환경 설정 & API 키 발급**
   Python 설치 확인, Google AI Studio에서 무료 API 키 발급, 가상환경 만들고 `google-generativeai`, `streamlit` 설치.

2. **도구 3개 함수 구현**
   분류/점수화/과장탐지 도구를 각각 파이썬 함수로 작성. ①②는 프롬프트로 Gemini에게 시키는 형태, ③은 단순 계산 로직.

3. **에이전트 서버(도구 선택 로직) 구현**
   Gemini function calling으로 위 3개 함수를 tools 스키마로 등록하고, 사용자 입력에 따라 모델이 필요한 도구를 호출하도록 연결. Streamlit UI에 입력창(리뷰 텍스트, 실제 별점)과 결과 출력 영역 구성.

4. **테스트 & 디버깅**
   실제 쇼핑몰 리뷰 2~3개로 정상 작동 확인, 무료 등급 rate limit 초과 여부 확인, 에러 처리. 실행 화면 스크린샷 확보 (제출물 ①).

5. **개발일지/README 작성**
   설치법, 코드 구조, API 키 설정법, 트러블슈팅(무료 등급 제한 이슈 등) 정리 (제출물 ②).

6. **직접 사용 후기 작성**
   완성본으로 실제 리뷰 3~5개 테스트, 장단점·아쉬운 점·개선하고 싶은 점 정리 (제출물 ③).

7. **깃허브 업로드**
   저장소 생성, 코드 + README + `.env.example`(API 키는 제외) + 커밋 이력 정리. 단계별로 나눠서 커밋 (제출물 ⑤).

8. **블로그/SNS 게시**
   브런치·벨로그·티스토리 중 하나에 개발 과정 + 후기 게시 (제출물 ④).

---

## 5. 평가 기준 대조표

| 항목 | 충족 방법 |
|---|---|
| ① 주제 구현 | 감성 분석기 + 3개 도구 완성 |
| ② 완성도 | 4단계 테스트로 정상 작동 확인 |
| ③ 문서화 | 5단계 README/개발일지 |
| ④ 실사용 후기 | 6단계 |
| ⑤ 블로그 게시 | 8단계 |
| ⑥ 깃허브 | 7단계 |

---

## 6. 주의사항 (실행 에이전트용)

- API 키는 절대 코드에 하드코딩하지 말고 환경변수 또는 `.env` + `.gitignore`로 관리할 것
- Gemini 무료 등급 rate limit에 걸리면 재시도 로직(exponential backoff) 또는 사용자에게 안내 메시지 표시
- 함수 호출 결과는 JSON으로 파싱해서 Streamlit 화면에 구조화된 형태(표, 카드 등)로 표시할 것
- 도구 ③은 LLM 호출 없이 순수 파이썬 계산으로 처리해 무료 등급 호출 횟수를 절약할 것
